{
  "name": "rocket-win",
  "version": "0.1.0",
  "description": "Rule-based proxy client for Windows, Shadowrocket-style UI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:svc": "node scripts/build-svc.mjs",
    "build:all": "node scripts/build-svc.mjs && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-shell": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "vite": "^5.4.10"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
